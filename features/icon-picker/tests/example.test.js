import { describe, it, expect } from 'vitest'

describe('IconPicker 重構測試基礎', () => {
  it('測試框架可以正常運作', () => {
    expect(true).toBe(true)
  })
  
  it('可以測試基本 JavaScript 功能', () => {
    const sum = (a, b) => a + b
    expect(sum(2, 3)).toBe(5)
  })
})